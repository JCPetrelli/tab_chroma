Disable the tab-chroma badge (the large background text in iTerm2).

Run the following and show the output:

```bash
~/.claude/hooks/tab-chroma/tab-chroma.sh badge off
~/.claude/hooks/tab-chroma/tab-chroma.sh reset
```
